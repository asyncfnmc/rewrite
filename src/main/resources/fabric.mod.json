{
  "schemaVersion": 1,
  "id": "rewrite",
  "version": "${version}",
  "name": "Rewrite",
  "description": "Create short, client-side aliases for server commands.",
  "authors": [
    "asyncfn"
  ],
  "license": "Source Available - Personal Use Only, All Rights Reserved",
  "environment": "client",
  "entrypoints": {
    "client": ["dev.sinister.rewrite.RewriteClient"],
    "modmenu": ["dev.sinister.rewrite.RewriteModMenu"]
  },
  "mixins": ["rewrite.client.mixins.json"],
  "depends": {
    "fabricloader": ">=0.19.3",
    "minecraft": "~26.1.2",
    "java": ">=25",
    "fabric-api": "*"
  },
  "suggests": { "modmenu": ">=18.0.0" }
}
